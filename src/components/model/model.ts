import AppView from '../view/appView';
import { CardItem } from '../types/types';

export class Model extends AppView {

  constructor() {
    super();
  }

  addTotalPrice() {
    let count = 0;
  }
}
